from setuptools import setup, find_packages

setup(
    name='external',
    version='1.0.0',
    url='',
    author='',
    author_email='',
    description='',
    packages=find_packages(), install_requires=['Pillow']
)
